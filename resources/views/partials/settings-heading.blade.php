<div class="flex flex-col gap-1">
    <h1 class="text-2xl font-bold text-zinc-900 dark:text-white">{{ __('Settings') }}</h1>
    <p class="text-sm text-zinc-600 dark:text-zinc-400">{{ __('Manage your profile and account settings') }}</p>
</div>
@include('partials.member-nav-tabs')
